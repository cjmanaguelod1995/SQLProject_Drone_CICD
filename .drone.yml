kind: pipeline
type: docker
name: PR Checker

steps:    

  - name: Build Sql Project
    image: didstopia/msbuild:latest
    commands:
    - docker build -t guid-generator-assets .
      
trigger:
  branch:
    - develop
