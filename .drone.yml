kind: pipeline
type: docker
name: PR Checker

steps:    

  - name: Build Sql Project
    image: hstntexans/ssdt-proj-builder
    commands:
    - dotnet build SQLProject_Drone_CICD.sln
      
trigger:
  branch:
    - develop
