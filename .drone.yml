kind: pipeline
type: docker
name: PR Checker

steps:    

  - name: Build Sql Project
    image: docker pull alexellisio/msbuild:12.0
    commands:
      - docker build -t guid-generator-assets .
      
trigger:
  branch:
    - develop
