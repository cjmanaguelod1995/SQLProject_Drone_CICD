kind: pipeline
type: docker
name: PR Checker

steps:    

  - name: Build Sql Project
    image: dockersamples/assets-db-builder
    commands:
    - dotnet build SQLProject_Drone_CICD.sln
      
trigger:
  branch:
    - develop
